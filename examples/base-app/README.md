## 使用



```bash

# 安装 http-server
yarn install

## 安装 `ezuikit-flv`, 拷贝 `node_modules/ezuikit-flv`下的 `decoder.js`, `decoder.wasm` 和 `index.js` 文件到当前目录下

## 运行
## http://localhost:8080
yarn run dev

```